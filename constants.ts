// The prompt has been moved to api/generate.ts for security purposes.
// This prevents users from inspecting the client code and injecting their own prompts.
export const HOLLYWOOD_SMILE_PROMPT = ""; 
